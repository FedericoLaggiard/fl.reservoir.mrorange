//import { FETCH_POSTS , FETCH_POST} from '../actions/index';

const INITIAL_STATE = {
  all: [],
  searchTerm: null
};

export default function(state = INITIAL_STATE, action){
  switch(action.type){
    default:
      return state;
  }
}
